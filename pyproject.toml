[project]
name = "ai-application-engineering"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "KAWAINO", email = "ino9009@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
ai-application-engineering = "ai_application_engineering:main"

[build-system]
requires = ["uv_build>=0.12.15,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
